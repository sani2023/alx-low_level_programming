#include <stdio.h>
/**
 * main -A program that print a line using the printf function
 * Return: 0 (Success)
 */
int main(void)
{
printf("With proper grammar. but the outcome is piece of ard,\n")
return (0);
}
